# 依赖清单（Python 3.11+）
# LLM
openai>=1.40.0
# 浏览器自动化（跨平台发布/评论互动）
playwright>=1.45.0
# 定时调度
apscheduler>=3.10.0
# HTTP（热搜抓取）
httpx>=0.27.0
# HTML 解析（热搜兜底）
beautifulsoup4>=4.12.0
lxml>=5.2.0
# 生图兜底（PIL 封面合成）
pillow>=10.3.0
# 配置
python-dotenv>=1.0.1
# tasks.yaml 解析
PyYAML>=6.0.1
# 测试
pytest>=8.2.0
pytest-asyncio>=0.23.0
# WebUI
flask>=3.0.0
